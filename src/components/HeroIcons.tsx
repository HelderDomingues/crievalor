
import React from "react";
import { Brain, BarChart3, Zap } from "lucide-react";
interface HeroIconsProps {
  isVisible: boolean;
}
const HeroIcons: React.FC<HeroIconsProps> = ({
  isVisible
}) => {
  // Retorna nulo para ocular todos os ícones
  return null;
};
export default HeroIcons;
