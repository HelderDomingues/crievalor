
# Configurações para o Netlify (funciona também com outras plataformas)

[build]
  publish = "dist"
  command = "npm run build"

[functions]
  node_bundler = "esbuild"

[dev]
  command = "npm run dev"
  targetPort = 8080
  port = 8888
  framework = "#auto"

# Redirect www to non-www (SEO canonicalização)
[[redirects]]
  from = "https://www.crievalor.com.br/*"
  to = "https://crievalor.com.br/:splat"
  status = 301
  force = true

# SPA fallback (deve ser o último)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# [[headers]]
#   for = "/*"
#   [headers.values]
#     X-Frame-Options = "DENY"
#     X-XSS-Protection = "1; mode=block"

# Evitar nosniff e CSP estritos no ambiente dev que quebram o Vite HMR e Modulos TS
# Se necessário, configurar isso especificamente no painel do Netlify em vez de no toml local para não quebrar o dev
